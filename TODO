TODO:
    - Make main_area prettier
    - Add option to filter per skin id
    - Add option to generate file with tabs for each skin
    - Make user-input check, don't allow empty input trigger
    - Tooltip on correct inputs (for example complete hostname.evo-games.com)
    - Add bet amounts of games ? - reward games api


# Mac/Linux
LOCAL_MODE=true python main.py

# Windows PowerShell
$env:LOCAL_MODE="true"; python main.py

# Windows CMD
set LOCAL_MODE=true && python main.py